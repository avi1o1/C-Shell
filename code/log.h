#ifndef LOG_H
#define LOG_H

/*
    Function to get logs of the shell
    @param cmndTokens: Tokenised command
    @return the string to be executed, or an empty string otherwise
*/
char* notTheMathLog(char* cmndTokens, char* logFilePath);

#endif